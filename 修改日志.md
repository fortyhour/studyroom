# 修改日志

## 1. 修复 auth.py 中 UserRole 导入错误

**问题**：`from app.models.role import Role, UserRole` — `UserRole` 类定义在 `user.py` 而非 `role.py`，导致导入失败。

**修改方案**：拆分为两行导入：
```python
from app.models.role import Role
from app.models.user import UserRole
```

---

## 2. 修复定时任务 Flask 应用上下文错误

**问题**：`scheduled_tasks.py` 中 `check_pending_violations`、`send_reminders`、`generate_daily_checkin_codes` 三个函数被 APScheduler 定时调用时运行在 Flask 应用上下文之外，导致 `SystemConfig.query` 等数据库操作抛出 `RuntimeError: Working outside of application context.`。

**修改方案**：
- 三个任务函数改为接收 `app` 参数，内部用 `with app.app_context():` 包裹所有数据库操作。
- `register_tasks` 改为接收 `app` 并通过 `args=[app]` 传递给各任务。
- `__init__.py` 调用处改为 `register_tasks(scheduler, app)`。

---

## 3. 修复 users.py 学生可越权访问管理员接口

**问题**：`GET /users`、`GET /users/{id}`、`PUT /users/{id}` 三个接口仅使用 `@jwt_required()`，任何登录用户（包括学生）都能访问用户管理功能。

**修改方案**：三个接口的装饰器改为 `@admin_required`，仅管理员/超级管理员可访问。移除不再需要的 `jwt_required`、`get_jwt_identity`、`super_admin_required` 导入。

---

## 4. 禁止删除管理员账号

**问题**：`DELETE /users/{id}` 接口可以删除拥有管理员或超级管理员角色的用户。

**修改方案**：删除前检查目标用户角色，若拥有「管理员」或「超级管理员」角色则返回 `403 不允许删除管理员账号`。

---

## 5. 禁止修改超级管理员的角色

**问题**：`POST /users/{id}/roles` 接口可以修改超级管理员的角色分配。

**修改方案**：分配前检查目标用户，若拥有「超级管理员」角色则返回 `403 不允许修改超级管理员的角色`。

---

## 6. 修复 roles.py 学生可越权访问角色管理接口

**问题**：`GET /roles` 和 `GET /roles/{id}/permissions` 两个接口仅使用 `@jwt_required()`，学生也能查看角色和权限详情。

**修改方案**：两个接口改为 `@admin_required`，仅管理员/超级管理员可访问。

---

## 7. 禁止修改超级管理员角色的权限

**问题**：`PUT /roles/{id}/permissions` 接口可以修改「超级管理员」角色的权限配置。

**修改方案**：设置权限前检查角色名，若为「超级管理员」则返回 `403 不允许修改超级管理员的权限`。

---

## 8. 修复多处接口权限漏洞

**问题**：在添加注释过程中发现以下接口权限控制不足：
- `PUT /seats/{id}` — 仅 `@jwt_required()`，学生可修改座位
- `GET /system-configs` — 仅 `@jwt_required()`，学生可查看系统配置
- `GET /statistics/overview` — 仅 `@jwt_required()`，学生可查看统计数据

**修改方案**：三个接口均改为 `@admin_required`。

---

## 9. 为所有路由文件添加接口注释

**问题**：12 个路由文件中接口缺少文档注释。

**修改方案**：为每个接口函数添加详细注释，包含：
- 完整 URL（如 `http://localhost:5000/api/v1/roles/{role_id}`）
- 请求方法（GET/POST/PUT/DELETE）
- 功能描述
- 所需权限
- 请求头格式
- 请求体 JSON 字段及必填/选填说明
- Query 参数说明
- 返回数据结构
- 特殊限制/校验规则

---

## 10. 系统配置值限制只能为数字

**问题**：`PUT /system-configs/{key}` 接口的 `config_value` 可以是任意字符串，可能导致后端解析异常。

**修改方案**：增加校验逻辑：
- 非数字值（如 `"abc"`）→ `400 配置值必须为数字`
- 非正整数（如 `0`、`-3`）→ `400 配置值必须为正整数`
- 正数字正常通过

---

## 11. 修复前端视图文件导入路径错误

**问题**：`AdminLayout.vue` 和 `StudentLayout.vue` 分别在 `views/admin/` 和 `views/student/` 子目录中，使用 `../stores/auth` 会错误解析到 `views/stores/auth`（不存在），导致 Vite 编译报错 `Failed to resolve import`。

**修改方案**：
- `views/admin/AdminLayout.vue`：`../stores/auth` → `../../stores/auth`
- `views/student/StudentLayout.vue`：`../stores/auth` → `../../stores/auth`

---

## 12. 修复个人中心角色显示为 JSON 对象的问题

**问题**：`/auth/login` 返回的 `roles` 是字符串数组（如 `["学生"]`），而 `/auth/me` 返回的是对象数组（如 `[{"role_id":1,"role_name":"学生"}]`）。前端 `fetchUser()` 刷新用户信息后，`{{ role }}` 直接渲染对象变成 JSON 字符串。

**修改方案**：
- Profile.vue 模板改为 `{{ role.role_name || role }}`，兼容两种格式。
- auth.js store 中 `isAdmin` 判断也改为兼容两种格式（`typeof r === 'string' ? r : r.role_name`）。

---

## 13. 新增学生修改自身邮箱的接口

**问题**：`PUT /users/{id}` 需要管理员权限，学生无法修改自己的邮箱。

**修改方案**：
- 后端新增 `PUT /api/v1/auth/me` 接口，任何登录用户可修改自己的邮箱。
- 前端 API 新增 `authAPI.updateMe(data)`。
- Profile.vue 的修改信息功能改为调用 `authAPI.updateMe` 而非 `userAPI.update`。

---

## 14. 学生端导航栏新增「返回管理端」按钮

**问题**：管理员进入学生端后，只能通过右上角下拉菜单点击「管理后台」返回，不够直观。

**修改方案**：在 `StudentLayout.vue` 导航栏右侧新增「返回管理端」按钮，仅管理员/超级管理员可见（`v-if="authStore.isAdmin"`），学生账号不显示。

---

## 15. 同自习室禁止创建编号重复的座位

**问题**：`POST /studyrooms/{room_id}/seats` 和 `PUT /seats/{id}` 没有座位编号唯一性校验，同自习室下可存在编号相同的座位。

**修改方案**：
- 创建座位时查询同自习室下是否已有相同编号的未删除座位，如有则返回 `400 该自习室下座位编号 X 已存在`。
- 更新座位编号时查询同自习室内除自身外是否有冲突，有则拒绝。

---

## 16. 前端编辑座位时禁止修改座位编号

**问题**：编辑座位弹窗中座位编号可修改，但注释中已标注「同一自习室内应唯一」且后端已限制，前端仍允许修改容易产生困惑。

**修改方案**：在 `SeatManage.vue` 的座位编号输入框添加 `:disabled="!!editing"`，编辑模式下禁止修改，仅新建时可输入。

---

## 17. 修复座位预约时间段无法选中的问题

**问题**：用户进入座位详情页后能看到空闲时间段，但无法选择预约起始和结束时间。经过多轮排查，根本原因是：

1. 初始版本中 `selectedSlots` 被定义为 `computed` 属性，每次重新计算都生成全新对象并重置状态，用户点击后选中效果丢失。
2. 改用直接操作 `slots` 数组的 `selected` 属性后，因 Vue 对新增属性的响应式追踪问题，选中仍无法正常触发高亮。
3. 改用 `Set<number>` 类型的 `selectedIndices` 后，逻辑层面已正确，但**后端返回的 `free_slots` 是合并后的连续区间**（如整天空闲时返回 `[{"start":"07:00","end":"23:00"}]` 一个整体块），前端直接渲染为单个不可拆分的大格子，用户无法从中选取子区间。

**修改方案**：
1. 移除 `computed` → 直接在 `slots` 中管理数据。
2. 使用 `Set<number>` 类型的 `selectedIndices` 记录选中索引，通过 `new Set(current)` 触发响应式更新，模板用 `selectedIndices.has(index)` 判断高亮。
3. 在 `fetchAvailability` 中将后端合并后的区间展开为逐小时格子：
   ```js
   for (const merged of res.data.free_slots) {
     const startH = parseInt(merged.start.split(':')[0])
     const endH = parseInt(merged.end.split(':')[0])
     for (let h = startH; h < endH; h++) {
       hourlySlots.push({
         start: `${String(h).padStart(2, '0')}:00`,
         end: `${String(h + 1).padStart(2, '0')}:00`
       })
     }
   }
   ```
   例如 `{"start":"07:00","end":"23:00"}` → 16 个独立小时格子，用户可逐个点击多选连续时段。

---

## 18. 已占用时段前端显示为红色且不可选

**问题**：后端原先只返回空闲时段（`free_slots`），已占用的整点时段不返回。前端只渲染空闲格子，用户看不到哪些时段已被预约，已被预约的时段直接"消失"。

**修改方案**：
- 后端 `GET /seats/{id}/availability` 接口改为返回全部整点时段，每项带 `free` 标记（`true`=空闲，`false`=已占用），字段名改为 `slots`（原 `free_slots`）。
- 前端 `SeatDetail.vue` 直接使用后端 `slots` 数据，移除客户端展开逻辑。CSS 增加三种状态样式：
  - `.free` — 绿色背景，可点击选中
  - `.occupied` — 红色背景，`cursor: not-allowed`，`toggleSlot` 中 `if (!slot.free) return` 禁止点击
  - `.selected` — 蓝色背景（选中态）
- 卡片标题改为「时间段（绿=空闲 红=已占用）」

---

## 19. 预约限制从"无未处理违约"改为"信誉分 > 0"

**问题**：原创建预约接口检查用户是否有 `VIOLATED` 状态的预约记录，如有则禁止预约。但违约扣分已通过定时任务自动扣除信誉分，双重惩罚不合理——用户已被扣分，还应能继续预约（只要信誉分尚未归零）。

**修改方案**：
- 移除 `POST /api/v1/reservations` 中查询 `VIOLATED` 状态预约的逻辑。
- 改为检查用户 `credit_score <= 0`，信誉分归零才禁止预约，提示"信誉分不足，无法预约"。
- 同步更新 README.md 中该接口的校验说明（"无未处理违约" → "信誉分 > 0"）。

---

## 20. 修复管理端和学生端页面下半部分被截掉的问题

**问题**：侧边栏使用 `min-height: 100vh`，内容区没有固定高度和独立滚动。当页面内容超出视口时，整体页面被撑高，但浏览器视口限制导致下半部分不可见且无法滚动。

**修改方案**：
- `AdminLayout.vue` 和 `StudentLayout.vue` 改为固定高度布局：
  - 外层容器 `height: 100vh; overflow: hidden` 锁定视口高度
  - `el-header` 添加 `flex-shrink: 0` 禁止被压缩
  - `el-main` 改用 `flex: 1; overflow-y: auto` 自动填满剩余空间（替换硬编码 `calc(100vh - 60px)`，消除 `el-header` 边框等导致的 1px 溢出）
  - `index.html` 添加 `html, body { margin:0; padding:0; height:100%; overflow:hidden }` 消除浏览器默认边距
  - 管理端侧边栏 `el-aside` 同样设置 `height: 100vh; overflow-y: auto` 支持菜单滚动

---

## 21. 管理端用户管理页新增编辑用户信息功能

**问题**：后端 `PUT /api/v1/users/{id}` 已支持修改邮箱和信誉分，但前端 UserManage.vue 没有编辑入口，管理员无法在线修改用户信息。

**修改方案**：
- 前端 `UserManage.vue` 操作列新增「编辑」按钮，点击弹出编辑弹窗。
- 弹窗包含邮箱（文本输入）和信誉分（数字输入 0-100）两个字段。
- 保存调用 `userAPI.update(id, {email, credit_score})`，保存成功后刷新列表。

---

## 22. 已关闭自习室禁止点击进入

**问题**：学生端首页自习室列表中，已关闭的自习室卡片仍可点击跳转到详情页。

**修改方案**：
- 将 `@click` 从直接路由跳转改为调用 `goRoom(room)` 方法，内部判断 `is_available`。
- 若已关闭，弹出 `ElMessage.warning('该自习室已关闭，无法进入')` 并 `return`。
- CSS 新增 `.room-disabled` 样式：`cursor: not-allowed; opacity: 0.6`，悬停取消上浮动画。

---

## 23. 新增预约日期范围限制（MAX_RESERVATION_DAYS）

**问题**：用户可选择任意未来日期进行预约，缺乏日期范围控制。管理员无法设置可预约的最远天数。

**修改方案**：
- 后端 `GET /seats/{id}/availability` 和 `POST /reservations` 两个接口新增日期校验：
  - 读取 `MAX_RESERVATION_DAYS` 系统配置（默认 7 天）
  - 不允许选择今天之前的日期
  - 不允许选择超过 `今天 + MAX_RESERVATION_DAYS` 的日期
- 后端新增 `GET /api/v1/system-configs/public` 公开接口（仅需登录，无需管理员权限），返回 `MAX_RESERVATION_DAYS` 供前端使用。
- 后端 `init_data.py` 新增 `MAX_RESERVATION_DAYS` 预置配置（默认值 7）。
- 前端 `SeatDetail.vue` 日期选择器添加 `:disabled-date` 属性：
  - 页面挂载时调用 `systemConfigAPI.getPublicConfigs()` 动态获取最大天数
  - `disabledDate` 函数禁用今天之前和超出范围的日期
- 前端 API 层新增 `systemConfigAPI.getPublicConfigs()` 方法。
- README.md 同步更新系统配置表、空闲时段接口和创建预约接口的说明。

---

## 24. 修复座位预约页默认日期时区偏差问题

**问题**：前端用 `new Date().toISOString().slice(0, 10)` 取 UTC 日期作为默认值，在 UTC+8 时区凌晨 0:00-8:00 时段，UTC 日期比本地日期晚一天，导致默认显示昨天的日期。后端用 `date.today()`（本地日期）校验，判定为"过去日期"并报错。

**修改方案**：`SeatDetail.vue` 改为用 `getFullYear/getMonth+1/getDate` 拼装本地日期字符串，与后端 `date.today()` 保持一致。

---

## 25. 自习室管理页新增查看签到码功能

**问题**：管理员在自习室管理页无法查看各自习室的今日签到码，需要到签到相关页面才能查看，不方便。

**修改方案**：
- `RoomManage.vue` 操作列新增「签到码」按钮（操作列宽度从 260 扩到 340）。
- 点击后弹出签到码弹窗，调用已有接口 `GET /api/v1/studyrooms/{id}/checkin-code` 获取今日签到码，以大号等宽字体展示。
- 弹窗底部提供「刷新签到码」按钮，调用 `POST /api/v1/studyrooms/{id}/checkin-code/refresh` 重新生成。
- 同时修复 `checkin.py`、`reservations.py`、`scheduled_tasks.py` 中签到码日期相关逻辑使用 `datetime.utcnow().date()` 导致的时区偏差，统一改为 `date.today()`。

---

## 26. 预约历史页新增签到时间校验与签到按钮

**问题**：PENDING 状态的预约需要能在操作列直接签到，但需校验当前时间是否在签到窗口内（开始时间 ~ 开始时间+宽限分钟数）。

**修改方案**：
- 后端 `GET /api/v1/system-configs/public` 新增返回 `CHECKIN_GRACE_MINUTES`，供前端读取宽限时间。
- 前端 `ReservationHistory.vue` 重写操作列：
  - PENDING 状态显示「签到」和「取消」两个按钮（移除了 ACTIVE 状态才出现的旧签到按钮逻辑，签到只在 PENDING 时做）
  - 页面挂载时调用 `systemConfigAPI.getPublicConfigs()` 获取 `CHECKIN_GRACE_MINUTES`（默认 15 分钟）
  - `canCheckin(row)` 判断 `now >= start_time 且 now <= start_time + graceMinutes`
  - 点击签到时：时间未到提示「签到时间未到」；超时提示「已超过签到宽限时间」；在窗口内则弹出签到码输入弹窗

---

## 27. 签到奖励与每日信誉分恢复机制

**问题**：缺乏正向激励，用户违约后信誉分只减不增，不利于长期运营。

**修改方案**：
- `POST /api/v1/reservations/{id}/checkin` 签到成功后，用户信誉分 +5（`min(100, credit_score + 5)`，不超过 100）。
- `scheduled_tasks.py` 新增 `daily_credit_reward` 定时任务，每日 0:00 给所有未删除用户信誉分 +1（不超过 100）。

---

## 28. 新增提前结束预约功能

**问题**：签到后状态变为 ACTIVE，用户在使用期间无法主动提前结束，只能等到 end_time 后由定时任务处理。实际场景中学生可能提前离开，需要手动结束释放座位。

**修改方案**：
- 后端新增 `PUT /api/v1/reservations/{id}/complete` 接口：仅本人可将 ACTIVE 状态的预约变为 COMPLETED。
- 前端 API 层新增 `reservationAPI.complete(id)` 方法。
- 前端 `ReservationHistory.vue` 操作列：ACTIVE 状态显示橙色「提前结束」按钮，点击弹出 `ElMessageBox.confirm` 二次确认后调用接口。

---

## 29. 当天已过时间段禁止预约

**问题**：选择当天日期时，已经过去的时间段仍显示为空闲可预约状态（如当前 11:36，10:00-11:00 仍为绿色可点击）。

**修改方案**：前端 `SeatDetail.vue` 的 `fetchAvailability` 中，若选择的日期等于今天，遍历所有 slot，将 `endHour <= nowHour`（整点结束时间已过）的 slot 的 `free` 强行覆盖为 `false`。后端已有数据不变，纯前端过滤，已过时间段自动变红且不可点击。

---

## 30. 已停用座位禁止点击进入

**问题**：自习室详情页中，`is_active=false` 的座位仍可点击跳转到预约页，管理员标记停用的座位学生仍能操作。

**修改方案**：
- `RoomDetail.vue` 将 `@click` 从直接路由跳转改为 `goSeat(seat)` 方法，内部判断 `is_active`，停用时弹出「该座位已停用」警告并阻断跳转。
- 停用座位卡片添加 `.disabled` class：灰色背景、`cursor: not-allowed`、`opacity: 0.5`、取消悬停动画。
- 停用座位的状态标签显示为蓝色「停用」而非占用/空闲。

---

## 31. 个人中心当前预约功能增强

**问题**：个人中心「当前预约」只显示详情和取消按钮，缺少签到和提前结束操作。且取消只支持 PENDING，ACTIVE 状态无法操作。

**修改方案**：
- `Profile.vue` 操作列重构，与预约历史页保持一致：PENDING→签到+取消，ACTIVE→提前结束。
- 新增签到弹窗、时间校验 (`canCheckin`)、签到码输入，完全复用 `ReservationHistory.vue` 的同款逻辑。
- 当前预约只查询 PENDING 和 ACTIVE 状态（不显示 COMPLETED/CANCELLED/VIOLATED 历史）。

---

## 32. 修复全局时间时区偏差及违约检测失效问题

**问题**：整个项目使用 `datetime.utcnow()` 存储和比较时间，在 UTC+8 时区偏差 8 小时。具体影响：
- 违约检测：`start_time <= now - checkin_grace` 中 `now` 为 UTC，`start_time` 为前端本地时间，8 小时偏差导致违约永远不触发或延迟触发。
- 预约记录 `created_at` 显示比实际晚 8 小时。
- 签到时间、提前结束时间同样偏差。
- 座位占用状态判断（`studyrooms.py` `seats.py`）也受影响。

**修改方案**：全项目将 `datetime.utcnow()` 统一替换为 `datetime.now()`，`default=datetime.utcnow` 也改为 `default=datetime.now`。涉及文件：
- `models/reservation.py`、`models/user.py`、`models/violation.py`、`models/checkin_code.py`（default 值）
- `routes/reservations.py`（签到、提前结束记录时间）
- `routes/studyrooms.py`（座位占用判断，3 处）
- `routes/seats.py`（座位列表占用判断、availability 默认日期，2 处）
- `utils/scheduled_tasks.py`（违约检测、提醒通知，2 处）

---

## 33. 预约历史与详情页新增「再次预约」功能

**问题**：已完成的预约需要能快速再次预约同一座位，而不用退回自习室列表重新找。已取消的预约详情页也缺少再次预约按钮。

**修改方案**：
- `ReservationHistory.vue` 操作列新增绿色「再次预约」按钮，COMPLETED/CANCELLED/VIOLATED 状态显示。
  - `reReserve(row)` 先查座位和自习室状态：座位停用则提示"该座位已停用"；自习室关闭则提示"该自习室已关闭"；均正常则跳转座位详情页。
- `ReservationDetail.vue`：再次预约按钮增加 CANCELLED 状态支持；同样加入座位/自习室可用性校验。

---

## 34. 修复管理员「返回管理端」按钮跳转失败

**问题**：管理员从个人中心点击「返回管理端」后无法正确跳转到 `/admin/dashboard`。根因：路由守卫 `router.beforeEach` 中 `isAdmin` 判断用 `r === '管理员'` 做字符串比较，但 `localStorage` 存储的 `user.roles` 是对象数组 `[{role_name: '管理员'}]`，导致角色判断失败，路由守卫把管理员重定向到 `/student/home`。

**修改方案**：`router/index.js` 守卫中的 `isAdmin` 改为和 `authStore.isAdmin` 一致的兼容逻辑，同时处理字符串数组和对象数组两种格式。

---

## 35. 座位标签「空闲」改为「开放中」，插座图标改中文文本

**修改方案**：
- `RoomDetail.vue`：`<el-icon><Connection /></el-icon>` 替换为 `<div class="power-tag">有插座</div>`，标签文字「空闲」改为「开放中」。
- `SeatDetail.vue`：时间段说明「绿=空闲」改为「绿=开放中」。

---

## 36. 首页 & 详情页「x/x 空闲」统计逻辑修正

**问题**：`total_seats` 只统计 `is_active=True` 的座位，停用座位被忽略导致数字偏小。`free_seats` = `total - occupied`，停用座位被算成空闲。

**修改方案**：`total_seats` 统计所有非删除座位（含停用）。`free_seats = total_seats - 停用数 - 占用数`。
- `studyrooms.py` 列表接口和详情接口同步修正。

---

## 37. 预约创建增加防冲突与日限额校验

**问题**：
1. 用户可在同一时间段预约两个不同座位。
2. 同一天可以无限次预约。

**修改方案**：在 `POST /reservations` 创建预约时（座位冲突检查之后）新增两段校验：
- **跨座位时间段冲突**：查询用户当天 PENDING/ACTIVE 预约中，是否有其他座位的时间段与本次重叠（`start_time < end_time AND end_time > start_time`），有则拒绝并提示「该时间段你已预约了其他座位」。
- **同天限额**：统计用户当天 PENDING/ACTIVE 预约数（按 `start_time` 日期），≥2 则拒绝并提示「同一天最多只能预约2次」。
