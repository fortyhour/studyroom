## 自习座位预约系统 — 开发过程与代码质量回顾

***

### 一、项目架构概览

```
project/
├── backend/                    # Python Flask 后端
│   ├── app/
│   │   ├── routes/             # 12 个蓝图路由（auth/users/roles/seats/...）
│   │   ├── models/             # 11 个数据模型
│   │   ├── utils/              # 装饰器 / 初始化数据 / 定时任务
│   │   └── config.py           # 配置管理
│   ├── test/                   # 8 个测试文件，93 个测试用例
│   └── run.py                  # 入口
├── front/                      # Vue 3 + Element Plus 前端
│   └── src/
│       ├── views/student/      # 6 个学生端页面
│       ├── views/admin/        # 8 个管理端页面
│       ├── api/                # Axios 接口封装
│       ├── stores/             # Pinia 状态管理
│       └── router/             # 路由守卫 + RBAC
├── databases/                  # SQLite 数据库
├── README.md                   # 完整文档
└── 修改日志.md                  # 37 条迭代记录
```

**技术栈**：Flask + SQLAlchemy + JWT + APScheduler（后端）、Vue 3 Composition API + Pinia + Element Plus + Vite（前端）

- **Flask**：轻量级 Python Web 框架，用于快速构建 Web 应用和 RESTful API，提供路由、请求处理、响应生成等核心功能。
- **SQLAlchemy**：Python SQL 工具包和对象关系映射（ORM）库，用于以面向对象的方式操作数据库，简化 SQL 查询、数据模型定义和事务管理。
- **JWT**：JSON Web Token 的缩写，一种安全的身份认证方案。后端生成并验证 Token，用于用户登录后的授权和接口访问控制。
- **APScheduler**：Python 的定时任务调度库，可在指定时间、间隔或周期执行后台任务，如数据清理、定期通知等。
- **Vue 3 Composition API**：Vue 3 提供的逻辑复用和代码组织方式，通过 `setup` 函数、响应式 API（如 `ref`、`reactive`）将组件的状态、方法按功能聚合，比旧版 Options API 更灵活。
- **Pinia**：Vue 官方推荐的状态管理库，用于存储全局共享数据（如用户信息、主题设置），支持模块化、持久化等功能，替代 Vuex。
- **Element Plus**：基于 Vue 3 的桌面端 UI 组件库，提供表格、表单、对话框等现成组件，帮助快速搭建统一美观的界面。
- **Vite**：新一代前端构建工具，利用原生 ES 模块提供极速的开发服务器热更新（HMR），并在生产打包时使用 Rollup 进行优化，比 Webpack 启动更快、构建更高效。

***

### 二、开发过程&#x20;

#### 阶段 1：基础搭建

全栈项目从零搭建：模型层 → 路由层 → 前端页面。后端采用Flask，前端采用Vue 3

#### 阶段 2：基于测试驱动的迭代

#### 一、权限加固

这是代码质量提升的关键，逐接口审查权限：

- 学生能访问 `GET /users`？→ 加上 `@admin_required`
- 管理员能删除超管账号？→ 禁止
- 超管的角色/权限能被修改？→ 禁止
- 系统配置能被改为非数字？→ 加正整数校验

#### 二、业务规则新增和完善

签到奖励、提前结束、跨座位冲突检测、日限额、停用座位阻断……这些是在功能可用的基础上逐步叠加的业务约束。

#### 三、前端优化

从 Vue 响应式问题（`computed` 丢失选中状态）到布局溢出（`100vh` vs `flex`），从 UTC 时区导致日期偏差到座位选择交互重构，每个问题都涉及前后端协同排查。

***

### 三、代码质量保障措施

#### 1. RBAC 权限模型（三级角色 + 12 权限码）

```
超级管理员 ─── 全部 12 个权限
    │
管理员     ─── 8 个管理权限（room:manage, seat:manage, user:manage...）
    │
学生       ─── 4 个基础权限（reservation:view, create, cancel, checkin）
```

权限控制实现层：

- `@admin_required` / `@super_admin_required` 装饰器做角色级拦截
- `@permission_required("seat:manage")` 做细粒度权限点拦截
- 前端路由守卫 `router.beforeEach` 读取 `localStorage` 做页面级拦截
- 双重防御：学生即使绕过前端直接调 API，后端装饰器仍会拒绝

#### 2. 输入校验分层

| 层级       | 校验内容                               |
| -------- | ---------------------------------- |
| **前端**   | 日期范围（`disabledDate`）、已过时段灰化、停用座位阻断 |
| **后端路由** | 参数完整性、整点校验、时长上下限、自习室开放时间匹配         |
| **后端业务** | 座位冲突、跨座位冲突、日限额、信誉分 > 0             |
| **模型层**  | 唯一约束（`username` unique）、默认值        |

```python
# 预约创建接口的校验链（reservations.py POST /reservations）
参数完整性 → 时间格式 → 整点校验 → 日期范围 → 时长限制
→ 座位可用性 → 自习室开放时间 → 同座位冲突 → 跨座位冲突 → 日限额 → 信誉分
```

#### 3. 时区一致性问题（#32）

这是项目中影响最广的修复。整个项目统一从 `datetime.utcnow()` 迁移到 `datetime.now()`：

```
涉及 10 个文件，16 处修改：
  models/    4 处：reservation, user, violation, checkin_code 的 default 值
  routes/    8 处：签到时间、提前结束时间、座位占用判断、可用性查询
  utils/     2 处：违约检测定时任务、提醒通知定时任务
```

如果不修，UTC+8 时区下违约检测会晚 8 小时触发，预约记录时间全部差 8 小时。

#### 4. 测试覆盖

**93 个测试用例**，覆盖 9 个核心模块：

| 模块         | 测试用例数  | 覆盖场景                                       |
| ---------- | ------ | ------------------------------------------ |
| 认证（auth）   | 10     | 注册成功/缺参/重名，登录成功/密码错/不存在，个人中心，登出            |
| 自习室        | 11     | CRUD + 列表筛选 + 权限拦截                         |
| 座位         | 13     | CRUD + 编号冲突 + 停用 + 可用时段查询                  |
| **预约（核心）** | **15** | 创建（含 8 种反向校验）+ 取消 + 列表筛选 + 详情 + 提前结束       |
| 签到         | 10     | 签到码生成/刷新 + 签到成功/码错/越权/重复 + 信誉分验证           |
| 用户管理       | 12     | 列表/搜索/分页 + 详情 + 邮箱/信誉分更新 + 删除管理员的保护 + 角色分配 |
| 角色管理       | 10     | CRUD + 系统角色保护 + 权限查看与设置                    |
| 违约记录       | 4      | 管理员全量/学生本人/用户筛选/分页                         |
| 系统配置       | 8      | 查询 + 更新（正数/非数字/零/负数/不存在键）+ 公开配置            |

正向 vs 反向测试比例约 **35:58**，反向测试覆盖了参数缺失、格式错误、权限不足、资源不存在、业务规则冲突、状态校验、越权操作等 7 种异常场景。

#### 5. 软删除 + 数据一致性

- 所有核心实体（用户、自习室、座位、预约）使用 `is_del=1` 软删除
- 删除管理员/系统角色时有业务级拦截
- 座位的 `total_seats` 在增删座位时联动更新
- 预约状态流转严格遵循状态机：

```
PENDING ──签到──▶ ACTIVE ──提前结束──▶ COMPLETED
   │                                       
   ├──取消──▶ CANCELLED                     
   │                                       
   └──超时未签到──▶ VIOLATED                 
```

目前已在华为云上部署了项目<http://123.60.19.172/login>

***

### 四、可以继续改进的方向

| 方向         | 建议                                |
| ---------- | --------------------------------- |
| **API 文档** | 可接入 Swagger/OpenAPI 自动生成          |
| **日志系统**   | 替换 `print()` 为 structured logging |
| **CI/CD**  | 在华为云上 自动跑 `pytest` 单元测试           |

